  <div class="container-fluid nav-bar bg-transparent">
            <nav class="navbar navbar-expand-lg bg-white navbar-light py-0 px-4">
                <a href="index.html" class="navbar-brand d-flex align-items-center text-center">
                    <div class="icon p-2 me-2">
                        <img class="img-fluid" src={{asset("img/icon-deal.png")}} alt="Icon" style="width: 30px; height: 30px;">
                    </div>
                    <h1 class="m-0 text-primary">Makaan</h1>
                </a>
                <button type="button" class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarCollapse">
                    <div class="navbar-nav ms-auto">
                        <a href={{path("app_participant")}} class="nav-item nav-link active">Home</a>
                        <div class="nav-item dropdown">
                            <a  class="nav-link dropdown-toggle" data-bs-toggle="dropdown">EVENEMENTS</a>
                            <div class="dropdown-menu rounded-0 m-0">
                                <a  class="dropdown-item">PASSER</a>
                                <a  class="dropdown-item">BIENTOT</a>
                            </div>
                        </div>
                        <a href="{{ path('app_user_reclamations', { 'id': app.user.id }) }}" class="nav-item nav-link">RECLAMATIONS</a>

                      
                    </div>
                    <a href="{{ path('app_edit_user', { 'id': app.user.id }) }}" class="btn btn-primary px-3 d-none d-lg-flex" style="margin-right:5px">Profile</a>
                    <a href={{path('app_logout')}} class="btn btn-danger px-3 d-none d-lg-flex">Logout</a>

                </div>
            </nav>
        </div>